programa

  numero a, b, numc.
  texto  t1, k, j.

  leia(a).
  leia(b).
  
  a := 1 + 2 * 3 / b.
  
  numc := j.

  se ( a > b ) {
     escreva (a).
  }
  senao {
     escreva (b).
  }
  

  leia(k).
  
   b := a.

fimprog.